## 2025-03-11 - [Optimize Date instantiation in loops]\n **Learning:** Creating a new Date object within a filter loop is highly inefficient. Extracting it to a variable outside the loop significantly reduces memory allocations and CPU cycles.\n **Action:** When working with Date objects inside loops, particularly `.filter()`, always initialize the current date outside the loop block and reference that variable inside the loop.
